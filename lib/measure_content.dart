import 'package:flutter/material.dart';

class MeasureContent extends StatelessWidget {
  const MeasureContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
